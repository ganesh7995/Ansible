plugin: amazon.aws.aws_ec2.yaml
regions:
- us-east-1

keyed_groups:
- key: tags.name
  prefix: "dynamic"
  separator: "_"
filters:
  tag:Name: frontend
  instance-state-name: running
hostnames:
  - private-ip-address
compose:
  ansible_host: private_ip_address